<ui:FluentWindow x:Class="Pendulum.App.Views.TriggerEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Reminder"
        Width="650"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="EditorTitleBar" Title="New Reminder" ShowMaximize="False" ShowMinimize="False"/>

        <StackPanel Grid.Row="1" Margin="28,4,28,28">

            <ui:TextBox x:Name="NameBox" PlaceholderText="Reminder name" FontSize="18" Margin="0,0,0,20"/>

            <ui:Card Margin="0,0,0,14">
                <StackPanel Margin="4">
                    <TextBlock Text="START DATE &amp; TIME" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,10"/>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="68"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="68"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="68"/>
                            <ColumnDefinition x:Name="AmPmSpacerColumn" Width="8"/>
                            <ColumnDefinition x:Name="AmPmColumn" Width="76"/>
                        </Grid.ColumnDefinitions>
                        <DatePicker x:Name="DatePickerControl" Grid.Column="0" VerticalAlignment="Center"/>
                        <ComboBox x:Name="HourBox" Grid.Column="2"/>
                        <ComboBox x:Name="MinuteBox" Grid.Column="4"/>
                        <ComboBox x:Name="SecondBox" Grid.Column="6"/>
                        <ComboBox x:Name="AmPmBox" Grid.Column="8" Padding="10,6"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ui:Button Grid.Column="0" Content="Repeat..." Appearance="Secondary" Click="RepeatButton_Click"/>
                        <TextBlock Grid.Column="1" x:Name="RecurrenceSummaryText" Text="Does not repeat" FontSize="12"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   VerticalAlignment="Center" Margin="12,0,0,0"/>
                    </Grid>

                    <Border x:Name="RecurrenceNotice" Background="{DynamicResource ControlFillColorSecondaryBrush}"
                            CornerRadius="6" Padding="10,8" Margin="0,12,0,0" Visibility="Collapsed">
                        <TextBlock FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                            <Run Text="&#xE72C;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"/>
                            <Run Text=" This reminder will automatically reschedule to its next occurrence when dismissed (not when snoozed)."/>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </ui:Card>

            <ui:Card Margin="0,0,0,14">
                <StackPanel Margin="4">
                    <TextBlock Text="ALERT TYPE" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,10"/>

                    <ComboBox x:Name="AlertTypeBox" Margin="0,0,0,16" SelectionChanged="AlertTypeBox_SelectionChanged"/>

                    <TextBlock x:Name="SoundLabel" Text="Sound" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                    <ComboBox x:Name="SoundBox" Margin="0,0,0,14"/>

                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Name="VolumeLabel" Text="Volume" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <TextBlock Grid.Column="1" x:Name="VolumeValueText" Text="100" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    </Grid>
                    <Slider x:Name="VolumeSlider" Minimum="0" Maximum="100" Value="100" Margin="0,0,0,14"
                            ValueChanged="VolumeSlider_ValueChanged"/>

                    <TextBlock x:Name="PhraseLabel" Text="Phrase to speak" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                    <ui:TextBox x:Name="PhraseBox" AcceptsReturn="True" TextWrapping="Wrap" Height="56"/>
                </StackPanel>
            </ui:Card>

            <CheckBox x:Name="EnabledBox" Margin="4,0,0,22"
                      Checked="EnabledBox_CheckedChanged" Unchecked="EnabledBox_CheckedChanged">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Enable"/>
                    <TextBlock x:Name="DisabledHint" Text=" (disabled)" Foreground="#FFE05252" Visibility="Collapsed"/>
                </StackPanel>
            </CheckBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0" Content="Test" Appearance="Secondary" Click="TestButton_Click"/>
                <ui:Button Grid.Column="2" Content="Cancel" Appearance="Secondary" Margin="0,0,10,0" Click="CancelButton_Click"/>
                <ui:Button Grid.Column="3" Content="Save Reminder" Appearance="Primary" Click="SaveButton_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
