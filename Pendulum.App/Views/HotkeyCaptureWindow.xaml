<ui:FluentWindow x:Class="Pendulum.App.Views.HotkeyCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Set Hotkey"
        Width="360"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Set Hotkey" ShowMaximize="False" ShowMinimize="False"/>

        <StackPanel Grid.Row="1" Margin="24,4,24,24">
            <TextBlock Text="Press the new key combination — it must include Ctrl, Alt, Shift, or Win."
                       FontSize="12" TextWrapping="Wrap"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,14"/>

            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="6" Padding="16,14" Margin="0,0,0,16">
                <TextBlock x:Name="CapturedText" Text="Press keys..." FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Cancel" Appearance="Secondary" Click="CancelButton_Click" Margin="0,0,10,0"/>
                <ui:Button x:Name="SaveButtonControl" Content="Save" Appearance="Primary" Click="SaveButton_Click" IsEnabled="False"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
