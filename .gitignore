# Build output
bin/
obj/
dist/
installer/publish/
installer/Output/

# User-specific files
*.user
*.suo
.vs/

# Rider / VS Code
.idea/
*.DotSettings.user

# OS cruft
Thumbs.db
.DS_Store

# Runtime-generated app data (only present if the exe has been run from the repo)
Data/

# Local archive of retired code/tooling — kept on disk, never in source control
ARCHIVE/

# Local-only cache of downloaded speech models (Whisper/Piper etc.) — kept on disk for
# testing/backup, never in source control (large binaries, not worth the repo/LFS cost).
resources/

# surmise.it landing site — lives in its own separate repo/deploy, not this one.
website/
